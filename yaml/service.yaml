apiVersion: v1
kind: Service
metadata:
  name: ginger-reservation-service
spec:
  selector:
    app: ginger-reservation-app
  ports:
    - port: 8080          # Service port
      targetPort: 8080   # Container port
      protocol: TCP
  type: LoadBalancer
